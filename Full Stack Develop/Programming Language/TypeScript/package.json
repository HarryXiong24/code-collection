{
  "name": "typescript-language-demo",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript language usage demos — types, collections, functions, classes/interfaces, generics, error handling, async, advanced types, decorators, reference semantics, standard library, testing.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.11.0"
  }
}
