# 复制为 .env 后填写。留空则使用括号里的默认值。
# 本地无认证：全部留空即可，直接连 http://localhost:9200
ES_NODE=
# 三选一的认证方式（填了 API Key 就忽略用户名密码）：
ES_API_KEY=
ES_USERNAME=
ES_PASSWORD=
# Elastic Cloud 用户填这个（填了它，ES_NODE 可以不填）：
ES_CLOUD_ID=
# 自签证书（本地开了 TLS 又懒得配 CA）时设为 false 跳过校验，生产别这么干：
ES_TLS_REJECT_UNAUTHORIZED=
